<nav class="navbar navbar-static-top bg-verde">

  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#expanded-menu" aria-expanded=" false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      

      <%= link_to 'IGGLE', root_url, class: 'navbar-brand white' %>
    </div>

  <!-- Conteudo exibido em telas maiores e que serao escondidos em telas menores -->
  <div class="collapse navbar-collapse" id="expanded-menu">  
    <ul class="nav navbar-nav navbar-right">

      <li>
        <%= link_to 'Nova Conta', cadastro_path, class: 'white' %>
      </li>

        <li class="white">
          <%= link_to 'Entrar', entrar_path, class: 'white' %>
        </li>

    </ul>

  </div>

  </div>

</nav>
      <div class="container">
          <% flash.each do |message_type, message| %>
            <%= content_tag 'div', class: "alert alert-#{message_type} alert-dismissible", role: 'alert' do %>
              <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
            <%= message %>
          <% end %>
        <% end %>
      </div>