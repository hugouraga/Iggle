<div class="col-md-6 col-md-offset-3 col-xs-12 container">
	<br><h3 class="center">Editar Horário</h3><br>
	<%= form_with(model: @schedule, local: true) do |form| %>

		<%= render "form", schedule: @schedule, form: form %>

		<div class="actions col-md-12">
		<div class="center">
			<%= form.submit 'Salvar', class: 'btn btn-verde', name: 'Salvar' %>
		</div>

		<div class='center'>
			<%= link_to 'Voltar', addresses_path, class: 'btn btn-cinza' %>
		</div>
		</div>

	<% end %>
</div>
