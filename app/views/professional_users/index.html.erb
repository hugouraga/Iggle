<br>
<div class="col-md-6 col-md-offset-3 col-xs-12 container">
    <div class="well well-large"> 
        <div class="form-inline center">
            <div class="form-group">
                <%= form_tag(professional_users_path, :method => :get) do %>
                    <%= text_field_tag 'search', nil, class: 'form-control form-style', placeholder:'Digite aqui sua busca'%>
                    <%= submit_tag 'Buscar', class:' btn btn-verde'%>
                <% end %>
                <br>
            </div>
        </div>

        <table class="table">
            <thead>
                <tr>
                <th>Nome</th>
                <th>Profissão</th>
                <th colspan="3"></th>
                </tr>
            </thead>

            <tbody>
                <% @professional_users.each do |professional_user| %>
                <tr>
                    <td><%= professional_user.name %></td>
                    <td><%= professional_user.profession %></td>
                    <td><%= link_to 'Visitar perfil', perfil_profissional_path %></td>
                </tr>
                <% end %>
            </tbody>
        </table>
    </div>
</div>