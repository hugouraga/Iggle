<%= render "layouts/header" %>

<div class="col-md-6 col-md-offset-3 col-xs-12 container">
  <h1>Cadastro</h1>
  <%= form_with(model: @professional_user, local: true) do |form| %>
    <%= render 'form', professional_user: @professional_user, form: form %>

    <div class="actions">
      <div class="center">
        <%= form.submit 'Cadastrar', class: 'btn btn-verde', name: 'Salvar' %>
      </div>

      <div class="center">
        <%= link_to 'Voltar', @professional_user, class: 'btn btn-cinza' %>
      </div>
    </div>
  <% end %>
</div>