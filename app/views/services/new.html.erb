<div class="col-md-6 col-md-offset-3 col-xs-12 container">
    <h1>Novo serviço</h1>
    <%= form_with(model: @service, local: true) do |form| %>
        <%= render 'form', service: @service, form: form %>

        <br>
        <div class="actions">
            <div class="center">
                <%= form.submit 'Adicionar', class: 'btn btn-verde' %>
            </div>

            <div class="center">
                <%= link_to 'Voltar', services_path, class: 'btn btn-cinza' %>
            </div>
        </div>
    <% end %>
</div>
